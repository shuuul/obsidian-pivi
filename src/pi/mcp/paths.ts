/** Vault-local MCP registry for Obsius (never reads ~/.config/mcp or host IDE configs). */
export const OBSIUS_MCP_CONFIG_PATH = '.obsius/mcp.json';

/** Previous location before Obsidian-specific vault layout. */
export const LEGACY_OBSIUS2_MCP_CONFIG_PATH = '.obsius2/mcp.json';

/** Vault-local OAuth tokens and PKCE state (never ~/.pi/agent/mcp-oauth). */
export const OBSIUS_MCP_OAUTH_DIR = '.obsius/mcp-oauth';
