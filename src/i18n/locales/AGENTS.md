# `src/i18n/locales/` — Contains JSON translation files for the Pivi plugin's internationalization (i1…

> Auto-generated by /init-deep (score: 20, depth: 3)

## Overview

This directory contains 10 files. Key file types: 10 .json.

## Content Analysis

**Purpose**: Contains JSON translation files for the Pivi plugin's internationalization (i18n) system. Each file provides localized strings for common UI elements, chat features (fork, bash mode), and settings sections — loaded at runtime by the i18n service with English as the default fallback.

**Key Files**:
- `en.json` — Primary locale (English, 316 lines); serves as the source of truth with all translation keys and default strings
- `ja.json` — Japanese translation; representative of CJK locales (also `zh-CN.json`, `zh-TW.json`, `ko.json`)
- `de.json` — German translation; representative of European locales (also `fr.json`, `es.json`, `pt.json`, `ru.json`)
- `../types.ts` — TypeScript union types `Locale` (10 codes) and `TranslationKey` (dot-path keys for type-safe lookups)
- `../constants.ts` — `SUPPORTED_LOCALES` array with native names, English names, and flag emojis per locale

**Patterns**: All 10 locale files share an identical JSON structure with three top-level namespaces (`common`, `chat`, `settings`) using dotted-key style. The `i18n.ts` service imports all JSON files as star-imports, maps them by locale code, and exposes type-safe `t(key, params)` with `{placeholder}` interpolation. `zh-CN.json` and `zh-TW.json` use hyphens in filenames (vs. underscores) to match BCP 47 locale tags.

## Files

| File | Type | Description |
|------|------|-------------|
| `de.json` | JSON | File |
| `en.json` | JSON | File |
| `es.json` | JSON | File |
| `fr.json` | JSON | File |
| `ja.json` | JSON | File |
| `ko.json` | JSON | File |
| `pt.json` | JSON | File |
| `ru.json` | JSON | File |
| `zh-CN.json` | JSON | File |
| `zh-TW.json` | JSON | File |
