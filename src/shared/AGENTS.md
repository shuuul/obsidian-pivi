# `src/shared/` — Contains reusable UI components, SVG icon utilities, mention autocomplete infras…

> Auto-generated by /init-deep (score: 11, depth: 2)

## Overview

This directory contains 1 files and 3 subdirectories. Key file types: 1 .ts.

## Content Analysis

**Purpose**: Contains reusable UI components, SVG icon utilities, mention autocomplete infrastructure, and modal dialogs shared across features. All modules remain provider-agnostic, importing only from `../../core/` — never from specific providers — consistent with the hexagonal architecture.

**Key Files**:
- `icons.ts` — SVG icon definitions (MCP, checkmark, Pi provider) and utility functions for constructing/appending SVG elements to the DOM
- `components/SelectableDropdown.ts` — Generic `SelectableDropdown<T>` class with keyboard navigation, selection highlighting, and hover/click event handling
- `mention/VaultMentionDataProvider.ts` — Caches vault files and folders in the background, exposing dirty-marking and retrieval methods for the `@mention` autocomplete
- `mention/types.ts` — Union type `MentionItem` covering file, folder, MCP server, context, agent, and agent-folder mention items, plus the `AgentMentionProvider` interface re-exported from core
- `modals/ForkTargetModal.ts` — Modal for choosing "new tab" or "current tab" as the fork target, exposed via the `chooseForkTarget()` promise-based factory

**Patterns**: Provider-agnostic imports (core types only, no provider references); generic class pattern (`SelectableDropdown<T>`) for reusable UI; promise-based modal factories (`chooseForkTarget`) that wrap `new Modal().open()`; background-initialized data providers with dirty-marking for caching.

## Files

| File | Type | Description |
|------|------|-------------|
| `icons.ts` | TypeScript | Icons module |

## Subdirectories

| Directory | Purpose |
|-----------|---------|
| `components/` | UI Components |
| `mention/` | Directory |
| `modals/` | Directory |
