# `src/core/types/` — Central type definitions layer for the Obsius plugin, implementing the hexagonal…

> Auto-generated by /init-deep (score: 28, depth: 3)

## Overview

This directory contains 9 files. Key file types: 9 .ts.

## Content Analysis

**Purpose**: Central type definitions layer for the Obsius plugin, implementing the hexagonal architecture's core domain contracts. Every file defines provider-neutral interfaces and types (chat, settings, tools, MCP, diff, agents, plugins) that the runtime adaptors and feature components depend on, with zero external library dependencies as required by the architecture guidelines.

**Key Files**:
- `index.ts` — Barrel file re-exporting all types from sibling modules
- `settings.ts` — `ObsiusSettings` and supporting types (permission modes, env snippets, slash commands, keyboard nav) — the single settings interface stored in `.obsius2/obsius2-settings.json`
- `chat.ts` — Conversation, ChatMessage, StreamChunk, ContentBlock, and UsageInfo types — the provider-agnostic messaging contract
- `mcp.ts` — MCP server configuration types (stdio/SSE/HTTP) plus validation helpers (`isValidMcpServerConfig`, `getMcpServerType`)
- `tools.ts` — Tool call tracking (ToolCallInfo, SubagentInfo, AskUserQuestionItem) and diff data for Write/Edit tools

**Patterns**: Zero-dependency domain types — no imports from outside `src/core/types/`. Strong use of discriminated unions (`StreamChunk`, `ContentBlock`, `McpServerConfig`) for type-safe branching. Settings use an index signature (`[key: string]: unknown`) for provider-extension fields. All types are `export type`-re-exported from the barrel to enforce type-only imports.

## Files

| File | Type | Description |
|------|------|-------------|
| `index.ts` | TypeScript | Module entry point |
| `agent.ts` | TypeScript | Agent module |
| `chat.ts` | TypeScript | Chat module |
| `diff.ts` | TypeScript | Diff module |
| `mcp.ts` | TypeScript | Mcp module |
| `plugins.ts` | TypeScript | Plugins module |
| `provider.ts` | TypeScript | Provider module |
| `settings.ts` | TypeScript | Settings module |
| `tools.ts` | TypeScript | Tools module |
