# `src/core/commands/` — Reserved command domain module

Currently an empty placeholder for future provider-neutral command-domain code. Existing slash command contracts live in `src/core/agent/commands/`.

## Rules

- Do not add feature UI or Pi adaptor dependencies here.
- Prefer `src/core/agent/commands/` for slash command catalog metadata unless this directory gains a distinct domain responsibility.
