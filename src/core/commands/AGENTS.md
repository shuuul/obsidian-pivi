# `src/core/commands/` — Reserved command domain module

Currently an empty placeholder for future pure command-domain code. Existing slash command contracts live in `src/core/agent/commands/`.

## Rules

- Do not add feature UI or low-level Pi SDK dependencies here.
- Prefer `src/core/agent/commands/` for slash command catalog metadata unless this directory gains a distinct domain responsibility.
