# `src/core/providers/` — The provider abstraction layer — the "ports" in the hexagonal architecture. Defi…

> Auto-generated by /init-deep (score: 17, depth: 3)

## Overview

This directory contains 7 files and 1 subdirectories. Key file types: 7 .ts.

## Content Analysis

## Directory Analysis: `src/core/providers/`

**Purpose**: The provider abstraction layer — the "ports" in the hexagonal architecture. Defines provider-neutral contract interfaces (`ProviderRegistration`, `ProviderChatUIConfig`, `ProviderWorkspaceServices`, etc.) that concrete providers (like the Pi adaptor in `src/providers/pi/`) must implement. Provides static registries for runtime dispatch of chat services, workspace services, environment variable routing, and settings reconciliation — all without importing any concrete provider.

**Key Files**:
- `types.ts` — Core contract interfaces: capabilities, chat UI config, settings reconciler, subagent lifecycle, title/instruction/inline-edit services, and workspace service types (commands, agents, MCP, CLI, settings tabs)
- `ProviderRegistry.ts` — Static registry dispatching chat-facing provider services (`createRuntime`, `getChatUIConfig`, `getSettingsReconciler`, etc.) by `ProviderId`; includes `RoutedTitleGenerationService` for multi-provider title generation
- `ProviderSettingsCoordinator.ts` — Projects per-provider model/reasoning/permission state into settings on provider switch, reconciles all providers on environment changes, and normalizes model variants
- `providerEnvironment.ts` — Classifies environment variables into shared vs. provider-owned scopes via key pattern matching, with migration from legacy flat storage to scoped storage
- `ProviderWorkspaceRegistry.ts` — Static registry for provider-owned workspace-level services (command catalogs, agent mention providers, CLI resolvers, MCP managers, settings tab renderers); `initializeAll` bootstraps all registered providers

**Patterns**: Static registry pattern (all registries are class-level statics, not instances — `ProviderRegistry` and `ProviderWorkspaceRegistry`); strict hexagonal seam (core defines only interfaces and registries, zero imports from `src/providers/`); scope-based environment variable routing with automatic key classification and review-flagging for unowned variables; snapshot/commit pattern for per-provider settings projection to avoid cross-provider leakage.

## Files

| File | Type | Description |
|------|------|-------------|
| `ProviderRegistry.ts` | TypeScript | ProviderRegistry module |
| `ProviderSettingsCoordinator.ts` | TypeScript | ProviderSettingsCoordinator module |
| `ProviderWorkspaceRegistry.ts` | TypeScript | ProviderWorkspaceRegistry module |
| `modelRouting.ts` | TypeScript | ModelRouting module |
| `providerConfig.ts` | TypeScript | ProviderConfig module |
| `providerEnvironment.ts` | TypeScript | ProviderEnvironment module |
| `types.ts` | TypeScript | Types module |

## Subdirectories

| Directory | Purpose |
|-----------|---------|
| `commands/` | Commands |
