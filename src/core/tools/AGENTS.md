# `src/core/tools/` — Provider-neutral tool taxonomy for Pi agent tool calls — a provid…

> Auto-generated by /init-deep (score: 10, depth: 3)

## Overview

This directory contains 5 files. Key file types: 5 .ts.

## Content Analysis

Here's the directory analysis:

---

**Purpose**: Defines the tool taxonomy for Pi agent tool calls — an agent-neutral catalog of tool names, type guards, input parsers, icon mappings, and result extractors used by the hexagonal core when rendering streamed tool activity.

**Key Files**:
- `toolNames.ts` — Central registry of all 30+ tool name constants (Read, Write, Bash, etc.) with categorized arrays (`EDIT_TOOLS`, `FILE_TOOLS`, `BASH_TOOLS`, `MCP_TOOLS`, `READ_ONLY_TOOLS`) and type guard functions
- `toolIcons.ts` — Maps each tool name to a Lucide icon identifier, with an `__mcp_icon__` marker for MCP-prefixed tools
- `toolInput.ts` — Parsers for tool inputs: extracts `AskUserAnswers` from structured results or fallback text, and resolves file paths from Read/Write/Edit/Glob/Grep/LS inputs
- `toolResultContent.ts` — Extractors normalizing tool result content (text blocks, arrays, JSON) into a plain string for display
- `todo.ts` — Parses TodoWrite tool input into typed `TodoItem[]` with validation, plus a reverse-scan to restore todos from saved conversation history

**Patterns**: Heavy use of `as const` for string literal types and discriminated unions. Categorized tool arrays (`EDIT_TOOLS`, `FILE_TOOLS`, etc.) paired with corresponding type guards (`isEditTool`, `isFileTool`) — a clean builder pattern for tool classification. Zero external dependencies, enforcing the hexagonal constraint that `src/core/` has no library imports.

## Files

| File | Type | Description |
|------|------|-------------|
| `todo.ts` | TypeScript | Todo module |
| `toolIcons.ts` | TypeScript | ToolIcons module |
| `toolInput.ts` | TypeScript | ToolInput module |
| `toolNames.ts` | TypeScript | ToolNames module |
| `toolResultContent.ts` | TypeScript | ToolResultContent module |
