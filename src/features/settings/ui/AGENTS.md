# `src/features/settings/ui/` — Provides Obsidian `Setting`-based UI components for the plugin's Settings tab. I…

> Auto-generated by /init-deep (score: 10, depth: 4)

## Overview

This directory contains 5 files. Key file types: 5 .ts.

## Content Analysis

Here is the structured summary:

**Purpose**: Provides Obsidian `Setting`-based UI components for the plugin's Settings tab. It manages three settings areas: environment variable configuration with snippet management, MCP server CRUD operations, and MCP server verification/testing — all built using Obsidian's native `Setting` components and modals.

**Key Files**:
- `EnvironmentSettingsSection.ts` — Renders a textarea for editing environment variables per scope (provider/shared), with a live "review ownership" warning and a context-limit container injected by the caller.
- `EnvSnippetManager.ts` — Manages reusable environment variable snippets: list/insert/edit/delete with an `EnvSnippetModal` that supports context limits per model ID, IME-safe keyboard shortcuts, and batch cross-scope insertion.
- `McpServerModal.ts` — Modal for adding/editing MCP servers with three protocol types (stdio/SSE/HTTP), dynamic field rendering per type, and clipboard import.
- `McpSettingsManager.ts` — Controller for the full MCP server list section — add/import/enable/disable/delete/test — with a self-contained dropdown add menu, optimistic save with rollback on failure, and bulk import for clipboard configs.
- `McpTestModal.ts` — Verification modal that connects to an MCP server, displays discovered tools with per-tool enable/disable toggles, a "disable all" bulk toggle, optimistic UI updates with rollback on persistence failure, and human-readable error messages.

**Patterns**: All UI components use Obsidian's modal and setting APIs directly (no framework). MCP components share a consistent pattern: modal for edit, manager for list CRUD, and dedicated test/verify modal. Environment snippets use a sub-modal pattern with IME-safe `Enter`/`Escape` keybindings. Persistence is handled via callbacks passed to modals, with optimistic UI updates and rollback on save failure.

## Files

| File | Type | Description |
|------|------|-------------|
| `EnvSnippetManager.ts` | TypeScript | EnvSnippetManager module |
| `EnvironmentSettingsSection.ts` | TypeScript | EnvironmentSettingsSection module |
| `McpServerModal.ts` | TypeScript | McpServerModal module |
| `McpSettingsManager.ts` | TypeScript | McpSettingsManager module |
| `McpTestModal.ts` | TypeScript | McpTestModal module |
