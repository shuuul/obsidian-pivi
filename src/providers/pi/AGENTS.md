# `src/providers/pi/` — This is the **Pi Coding Agent adaptor** — the sole provider adaptor in the Obsiu…

> Auto-generated by /init-deep (score: 29, depth: 3)

## Overview

This directory contains 3 files and 6 subdirectories. Key file types: 3 .ts.

## Content Analysis

## Analysis: `src/providers/pi/`

**Purpose**: This is the **Pi Coding Agent adaptor** — the sole provider adaptor in the Obsius2 plugin (hexagonal architecture). It wraps Pi's JSON-RPC subprocess runtime (`pi --mode rpc`) behind the provider-neutral interfaces defined in `src/core/`. Every file here exports entities that conform to core provider types.

**Key Files**:
- `registration.ts` — Wires all Pi services into a single `ProviderRegistration` object (runtime, settings, UI config, auxiliary services, capabilities)
- `runtime/PiChatRuntime.ts` — Core Pi runtime: manages the pi subprocess lifecycle (spawn, stdin/stdout JSON-RPC transport), session creation, prompt/stream pipeline, and session notification handling
- `settings.ts` — Defines `PersistedPiProviderSettings` and `PiProviderSettings` schemas, with merge logic for provider config, environment variables, and defaults
- `capabilities.ts` — Declares Pi's feature flags (persistent runtime, native history, plan mode, instruction mode, reasoning effort control)
- `protocol/` — Inline Pi RPC protocol layer: `PiSubprocess`, `PiJsonRpcTransport`, `PiClientConnection`, `PiSessionUpdateNormalizer`, and protocol types
- `ui/PiChatUIConfig.ts` — UI configuration: model option generation (pi-default + dynamically cached pi-ai models), permission mode toggle (Safe/YOLO/Plan), icon

**Patterns**:
- All files export modular service objects that conform to core interfaces — zero direct imports from other providers
- Subprocess lifecycle managed via lightweight JSON-RPC abstractions (`PiSubprocess` → `PiJsonRpcTransport` → `PiClientConnection`) with deterministic cleanup
- Settings flow through `getProviderConfig`/`setProviderConfig` core helpers; env vars parsed from multiline text
- Auxiliary services (inline edit, instruction refinement, title generation, task result interpretation) currently have stub implementations, indicating planned expansion

## Files

| File | Type | Description |
|------|------|-------------|
| `capabilities.ts` | TypeScript | Capabilities module |
| `registration.ts` | TypeScript | Registration module |
| `settings.ts` | TypeScript | Settings module |

## Subdirectories

| Directory | Purpose |
|-----------|---------|
| `app/` | Directory |
| `auxiliary/` | Directory |
| `env/` | Directory |
| `history/` | Directory |
| `runtime/` | Directory |
| `ui/` | UI Layer |
