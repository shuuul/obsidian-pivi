# `src/providers/pi/` — Pi agent adaptor (`pi-agent-core`) — sole provider in Obsius

> Auto-generated by /init-deep (score: 29, depth: 3); manually updated for in-process runtime.

## Overview

This directory contains 3 files and 6 subdirectories. Key file types: 3 .ts.

## Content Analysis

## Analysis: `src/providers/pi/`

**Purpose**: This is the **Pi agent adaptor** — the sole provider adaptor in the Obsius2 plugin (hexagonal architecture). It runs an in-process `Agent` from `@earendil-works/pi-agent-core`, streams turns via `@earendil-works/pi-ai`, and exposes everything through provider-neutral interfaces in `src/core/`. Every file here exports entities that conform to core provider types.

**Key Files**:
- `registration.ts` — Wires all Pi services into a single `ProviderRegistration` object (runtime, settings, UI config, auxiliary services, capabilities)
- `runtime/PiChatRuntime.ts` — Core Pi runtime: creates/reloads the in-process agent, resolves models from `pi-ai`, runs prompt/stream turns, and queues `StreamChunk`s for the feature layer
- `runtime/PiAgentEventAdapter.ts` — Maps `pi-agent-core` agent events into Obsius `StreamChunk`s (text, tool calls, thinking, errors, done)
- `settings.ts` — Defines `PersistedPiProviderSettings` and `PiProviderSettings` schemas, with merge logic for provider config, environment variables, and defaults
- `capabilities.ts` — Declares Pi's feature flags (persistent runtime, native history, plan mode, instruction mode, reasoning effort control)
- `ui/PiChatUIConfig.ts` — UI configuration: model option generation (pi-default + dynamically cached pi-ai models), permission mode toggle (Safe/YOLO/Plan), icon

**Patterns**:
- All files export modular service objects that conform to core interfaces — zero direct imports from other providers
- In-process agent lifecycle: `PiChatRuntime` owns the `Agent` instance and recreates it when model or env vars change
- Settings flow through `getProviderConfig`/`setProviderConfig` core helpers; env vars parsed from multiline text
- Auxiliary services (inline edit, instruction refinement, title generation, task result interpretation) are provider-specific implementations behind core ports

## Files

| File | Type | Description |
|------|------|-------------|
| `capabilities.ts` | TypeScript | Capabilities module |
| `registration.ts` | TypeScript | Registration module |
| `settings.ts` | TypeScript | Settings module |

## Subdirectories

| Directory | Purpose |
|-----------|---------|
| `app/` | Directory |
| `auxiliary/` | Directory |
| `env/` | Directory |
| `history/` | Directory |
| `runtime/` | Directory |
| `ui/` | UI Layer |
