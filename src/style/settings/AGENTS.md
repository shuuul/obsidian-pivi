# `src/style/settings/` — These 7 CSS files style the entire Obsidian settings interface for the obsius2 p…

> Auto-generated by /init-deep (score: 14, depth: 3)

## Overview

This directory contains 7 files. Key file types: 7 .css.

## Content Analysis

**Purpose**: These 7 CSS files style the entire Obsidian settings interface for the obsius2 plugin. `base.css` provides the foundational layout (tab navigation, provider cards, shared panel patterns, auth controls, model grids) while each additional file scopes overrides to a specific settings tab (plugins, MCP servers, slash commands, environment snippets, OpenCode model picker). All files use Obsidian CSS custom properties for seamless theme integration.

**Key Files**:
- `base.css` — Foundation with tab navigation, provider cards with expandable detail, shared `.obsius2-sp-*` panel layout, auth toggles, model checklist grid, hotkey grid, and validation/error states (510+ lines)
- `plugin-settings.css` — Plugin management list with status indicators (green/muted/red dots), version badges, and inline action buttons
- `mcp-settings.css` — MCP server list with add-dropdown menu, status dots, type badges, and per-item hover controls
- `env-snippets.css` — Environment snippet CRUD with context limits UI, preview rendering, and a full snippet modal
- `opencode-model-picker.css` — Multi-provider model selection with search, provider filter, selected-row aliasing, and unavailable model indicators

**Patterns**: Every file follows a consistent `obsius2-{feature}-{element}` naming convention, with `base.css` owning shared structural classes (`.obsius2-sp-*` for settings panels, `.obsius2-settings-action-btn` for icon buttons) that per-tab files extend. Compound states use BEM-like `--` modifiers (e.g., `--active`, `--disabled`, `--error`). Interactive elements consistently use `0.15s ease` transitions.

## Files

| File | Type | Description |
|------|------|-------------|
| `agent-settings.css` | CSS | File |
| `base.css` | CSS | File |
| `env-snippets.css` | CSS | File |
| `mcp-settings.css` | CSS | File |
| `opencode-model-picker.css` | CSS | File |
| `plugin-settings.css` | CSS | File |
| `slash-settings.css` | CSS | File |
