# `src/style/settings/` — Pi settings UI styles (6 CSS modules)

> Auto-generated by /init-deep (score: 14, depth: 3)

## Overview

This directory contains 6 files. Key file types: 6 .css.

## Content Analysis

**Purpose**: These 6 CSS files style the Obsius settings UI (Pi adaptor tabs). `base.css` provides tab navigation, pi-ai provider cards, shared panel layout, auth controls, and model grids; each additional file scopes a settings section (plugins, MCP, slash commands, environment snippets, agent config). All files use Obsidian CSS custom properties for theme integration.

**Key Files**:
- `base.css` — Tab navigation, `.obsius2-provider-card` for pi-ai backends, shared `.obsius2-sp-*` panel layout, auth toggles, model checklist grid, hotkey grid, validation states
- `plugin-settings.css` — Plugin management list with status indicators (green/muted/red dots), version badges, and inline action buttons
- `mcp-settings.css` — MCP server list with add-dropdown menu, status dots, type badges, and per-item hover controls
- `env-snippets.css` — Environment snippet CRUD with context limits UI, preview rendering, and a full snippet modal
- `agent-settings.css` — Pi agent configuration panels
- `slash-settings.css` — Slash command visibility and management UI

**Patterns**: Every file follows a consistent `obsius2-{feature}-{element}` naming convention, with `base.css` owning shared structural classes (`.obsius2-sp-*` for settings panels, `.obsius2-settings-action-btn` for icon buttons) that per-tab files extend. Compound states use BEM-like `--` modifiers (e.g., `--active`, `--disabled`, `--error`). Interactive elements consistently use `0.15s ease` transitions.

## Files

| File | Type | Description |
|------|------|-------------|
| `agent-settings.css` | CSS | File |
| `base.css` | CSS | File |
| `env-snippets.css` | CSS | File |
| `mcp-settings.css` | CSS | File |
| `plugin-settings.css` | CSS | File |
| `slash-settings.css` | CSS | File |
