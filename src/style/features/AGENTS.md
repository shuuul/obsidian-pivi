# `src/style/features/` — This directory contains feature-specific CSS modules for the Obsius plugin's int…

> Auto-generated by /init-deep (score: 22, depth: 3)

## Overview

This directory contains 11 files. Key file types: 11 .css.

## Content Analysis

**Purpose**: This directory contains feature-specific CSS modules for the Obsius plugin's interactive capabilities. Each file styles a distinct user-facing feature — from inline editing and diff review to plan-mode approval cards and image modals — all imported via `src/style/index.css` as part of the `npm run build:css` pipeline.

**Key Files**:
- `inline-edit.css` — Styles the CM6 inline chat input widget, selection highlights (CSS Custom Highlight API), agent reply box, and inline diff accept/reject buttons with green/red theme colors.
- `diff.css` — Styles write/edit diff blocks with colored insert/delete line highlighting, hunk separators, status indicators (completed/error/blocked), and line-prefixed unified diff display.
- `plan-mode.css` — Styles inline plan approval cards with scrollable content preview, permissions list, and a teal border/shadow input wrapper for plan-mode state.
- `image-modal.css` — Styles a full-screen dark overlay image modal with centered responsive image, close button, and box-shadow transitions.
- `resume-session.css` — Styles for session resume prompts (inferred from import order and file name; listed in `index.css`).

**Patterns**:
- **Prefix**: All selectors use `.obsius2-` prefix (adapting the CLAUDE.md `.claudian-` convention to the plugin's Obsidian ID `obsius2`).
- **Theme-agnostic**: Relies entirely on Obsidian CSS variables (`--text-normal`, `--background-modifier-border`, `--color-red`, etc.) — no hardcoded colors, enabling dark/light theme compatibility.
- **BEM-lite naming**: Blocks like `obsius2-inline-input`, elements like `obsius2-inline-input-container`, and status modifier classes (`.status-completed`, `.status-error`).
- **Zero external dependencies**: Pure CSS, single-file-per-feature, ordered by `@import` in `index.css`.

## Files

| File | Type | Description |
|------|------|-------------|
| `ask-user-question.css` | CSS | File |
| `diff.css` | CSS | File |
| `file-context.css` | CSS | File |
| `file-link.css` | CSS | File |
| `image-context.css` | CSS | File |
| `image-embed.css` | CSS | File |
| `image-modal.css` | CSS | File |
| `inline-edit.css` | CSS | File |
| `plan-mode.css` | CSS | File |
| `resume-session.css` | CSS | File |
| `slash-commands.css` | CSS | File |
