# `src/style/components/` — Contains all CSS component modules for the Obsius2 plugin's chat UI, implementin…

> Auto-generated by /init-deep (score: 24, depth: 3)

## Overview

This directory contains 12 files. Key file types: 12 .css.

## Content Analysis

**Purpose**: Contains all CSS component modules for the Obsius2 plugin's chat UI, implementing the Pi agent sidebar's visual interface inside Obsidian. Each file scopes styles under the `.obsius2-*` namespace to avoid conflicts with Obsidian's own styles.

**Key Files**:
- `messages.css` — Chat message layout: user/assistant bubbles, welcome screen, text blocks, scrollbar styling, and inline copy buttons
- `input.css` — Input area with textarea, file chips (context attachments), mode indicators (instruction/bash), toolbar, and composer queue UI
- `toolcalls.css` — Tool call rendering: collapsible headers, status icons (running/completed/error/blocked), output lines, search links, and patch/results display
- `code.css` — Code blocks with language labels, copy buttons (hover-reveal), and light/dark theme background adjustments
- `tabs.css` — Tab bar badges with streaming/attention/idle states and per-provider border coloring (claude/codex/opencode)

**Patterns**:
- **Consistent `obsius2-` namespace prefix** on all selectors for clean isolation from Obsidian's theme
- **`--text-muted` / `--background-*` CSS variable usage** ties into Obsidian's theme system for automatic light/dark mode support
- **Collapsible accordion pattern** shared across `toolcalls.css`, `thinking.css`, `subagent.css`, and `status-panel.css` — header with click toggle, bordered content indentation, and sibling gap spacing via adjacent combinators
- **Reveal-on-hover pattern** for secondary actions (copy buttons, message actions, history item actions) using `opacity: 0` + `:hover` transitions

## Files

| File | Type | Description |
|------|------|-------------|
| `code.css` | CSS | File |
| `context-footer.css` | CSS | File |
| `header.css` | CSS | File |
| `history.css` | CSS | File |
| `input.css` | CSS | File |
| `messages.css` | CSS | File |
| `nav-sidebar.css` | CSS | File |
| `status-panel.css` | CSS | File |
| `subagent.css` | CSS | File |
| `tabs.css` | CSS | File |
| `thinking.css` | CSS | File |
| `toolcalls.css` | CSS | File |
